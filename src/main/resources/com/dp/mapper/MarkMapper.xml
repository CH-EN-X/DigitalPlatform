<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dp.mapper.MarkMapper">

  <!--  int insert(Mark mark);-->
    <insert id="insert"  useGeneratedKeys="true" keyProperty="mId" databaseId="mysql">
        insert into mark (u_id,p_id,mark_details,rating) values(#{uId},#{pId},#{markDetails},#{rating})
    </insert>


    <!--  public List<Mark> finds(Map<String,Object> map);-->
    <select id="finds" resultType="Mark">

        SELECT * FROM mark JOIN `user` ON `user`.u_id = mark.u_id
        <where>
            <if test="pId!=null">
                and p_id = #{pId}
            </if>



        </where>


    </select>


    <!--  public List<ProdDetails> find(Map<String,Object> map);-->
    <select id="find" resultType="ProdDetails">


        SELECT * FROM  prod_details

        <where>
            <if test="pId!=null">
                and p_id = #{pId}
            </if>



        </where>



    </select>



</mapper>